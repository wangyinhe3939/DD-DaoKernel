# DD-支付回调幂等

问题：支付回调偶尔重复入账。

分类：Workflow / MEDIUM。

证：确认真实重复触发链与当前无业务幂等键。

名：问题不是“支付系统架构差”，而是“重复回调缺少幂等保护”。

为：增加业务幂等键与回归测试。

界：不碰历史账务修复，不做数据迁移。

验：同一回调重复到达只入账一次。

止：PASS 后立即 STOP。

当前不做：repository 重构、依赖升级、日志体系重写。
